apiVersion: v1
kind: Service
metadata:
  name: hgjgukk
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "hgjgukk"
    port: 8080
  selector:
    app: hgjgukk
