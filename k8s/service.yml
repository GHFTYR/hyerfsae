apiVersion: v1
kind: Service
metadata:
  name: hyerfsae
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "hyerfsae"
    port: 8080
  selector:
    app: hyerfsae
